#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i, j, m = 5, n = 5, o = 5, p = 5, M1[m][n], M2[o][p];
    for (i = 0; i < n; i++)
    {
        for (j = 0; j < n; j++)
        {
            printf("entrez un nombre");
            scanf("%d", &T[i][j]);
            printf("\n");
            printf("%d", i);
            printf("\n");
        }
    }
}